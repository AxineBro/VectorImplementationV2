# Реализация класса Vector с интерактивной консолью

Пользовательский шаблонный класс Vector на C++, предоставляющий функциональность динамического массива с дополнительными математическими операциями и полноценной интерактивной консолью для управления векторами.

## 🔥 Новые возможности

- **Интерактивная консоль** - Полноценное меню для создания и управления векторами
- **Поддержка множества типов данных** - int, long, double, float, char, std::string
- **Работа с несколькими векторами** - Создание, переключение, удаление векторов
- **Безопасный ввод данных** - Проверка границ и обработка ошибок
- **Автоматическое определение типов** - Умные операции между векторами разных типов

## 📋 Обзор класса

Класс `Vector<T>` предоставляет следующие основные функции:

### Конструкторы
- `Vector()` - Конструктор по умолчанию (ёмкость: 10)
- `Vector(size_t initial_size)` - Конструктор с начальным размером
- `Vector(const Vector& other)` - Конструктор копирования

### Основные операции
- `push_back(const T& val)` - Добавить элемент в конец
- `push_front(const T& val)` - Добавить элемент в начало
- `insert(size_t pos, const T& val)` - Вставить элемент в позицию
- `erase(size_t pos)` - Удалить элемент в позиции
- `get_size()` - Получить текущее количество элементов

### Математические операции
- `operator+` - Поэлементное сложение векторов
- `operator*` - Поэлементное умножение векторов
- `operator==` - Сравнение векторов на равенство

## 🎮 Интерактивная консоль

### Меню операций
```
=== Vector Operations Menu ===
1. Create new vector
2. Push back element
3. Push front element
4. Insert element at position
5. Erase element at position
6. Display current vector
7. Vector addition
8. Vector multiplication
9. Compare vectors
10. Show all vectors
11. Change current vector
12. Delete vector
0. Exit
```

### Поддерживаемые типы данных
- **int** - Целые числа
- **long** - Длинные целые числа
- **double** - Числа с плавающей точкой двойной точности
- **float** - Числа с плавающей точкой
- **char** - Символы
- **string** - Строки

## 💻 Примеры использования через консоль

### Создание вектора
```
Select type:
1 - int
2 - long
3 - double
4 - float
5 - char
6 - string
Enter choice: 1
Enter initial size (0 for empty): 3
Created vector #0
```

### Добавление элементов
```
Choose an option: 2
Enter value to push_back: 42
Result: [0, 0, 0, 42]
```

### Математические операции
```
Choose an option: 7
All vectors:
Vector #0 [int]: [1, 2, 3] (size: 3) [CURRENT]
Vector #1 [int]: [4, 5, 6] (size: 3)
Enter first vector index (0-1): 0
Enter second vector index (0-1): 1
Result saved as vector #2
```

### Сравнение векторов
```
Choose an option: 9
All vectors:
Vector #0 [int]: [1, 2, 3] (size: 3) [CURRENT]
Vector #1 [int]: [1, 2, 3] (size: 3)
Enter first vector index (0-1): 0
Enter second vector index (0-1): 1
Equal
```

## 🛠 Технические особенности

### Улучшенная система типов
```cpp
template <typename U>
    requires requires (T a, U b) { a + b; }
auto operator+(const Vector<U>& other) const {
    using R = decltype(std::declval<T>() + std::declval<U>());
    // Автоматическое определение результирующего типа
}
```

### Безопасный доступ к векторам
```cpp
template <typename Func>
void visitCurrentVector(std::vector<VectorVariant>& vectors, int currentIndex, Func func) {
    if (currentIndex < 0 || currentIndex >= static_cast<int>(vectors.size())) {
        std::cout << "No vector selected!\n";
        return;
    }
    std::visit(func, vectors[currentIndex]);
}
```

### Управление памятью
- Автоматическое увеличение ёмкости при переполнении
- Глубокое копирование при операциях присваивания
- Правильная обработка исключений

## 📁 Структура файлов

- `Vector.h` - Заголовочный файл с реализацией шаблона класса Vector
- `main.cpp` - Интерактивная консоль для работы с векторами

## ⚠️ Важные заметки

- **Проверка размеров**: Математические операции требуют векторы одинакового размера
- **Совместимость типов**: Операции возможны только между совместимыми типами данных
- **Текущий вектор**: Все операции применяются к текущему выбранному вектору
- **Безопасность**: Проверка границ при доступе к элементам

## 🚀 Запуск программы

```bash
g++ -std=c++20 main.cpp -o vector_program
./vector_program
```

## 🔧 Требования

- C++20 или новее

## 💡 Особенности реализации

- **SFINAE и концепты** - Для проверки поддерживаемых операций между типами
- **std::variant** - Для хранения векторов разных типов в одной коллекции
- **Шаблонные лямбды** - Для типобезопасных операций с любыми типами векторов
- **Умные указатели** - Для автоматического управления памятью

Данная реализация демонстрирует современные возможности C++20 и предоставляет удобный интерактивный интерфейс для работы с пользовательскими векторами.
